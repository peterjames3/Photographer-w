import { Mulish, Cormorant } from 'next/font/google';
export const cormorant = Cormorant({
    subsets: ['latin'],
    weight:['400', '500']
})

export const mulish = Mulish({
    subsets: ['latin'],
    weight:['300', '400',]
})